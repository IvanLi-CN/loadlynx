[package]
name = "loadlynx-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
heapless = { version = "0.8", default-features = false }
minicbor = { version = "0.24", default-features = false, features = ["derive"] }
