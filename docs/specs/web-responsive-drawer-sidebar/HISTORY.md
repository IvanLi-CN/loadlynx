# History

- Legacy compatibility identity: `#t4zh9`.

## Origin

- Migrated from legacy planning docs into the canonical specs taxonomy.

## Key Decisions

- Preserve the legacy spec ID `0012` and slug `web-responsive-drawer-sidebar` for traceability.
- Keep the original planning scope traceable while assigning long-lived requirements to `SPEC.md` and implementation/history records to companion documents.
- 2026-06-20: Superseded by `web-top-nav-device-workspace`; the active shell no longer uses a sidebar, icon rail, or navigation drawer.

## Documentation Model

`SPEC.md` is retained as the superseded sidebar/drawer contract. Current shell requirements live in `docs/specs/web-top-nav-device-workspace/SPEC.md`.
