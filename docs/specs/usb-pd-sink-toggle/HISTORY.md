# History

- Legacy compatibility identity: `#h3gz5`.

## Origin

- Migrated from legacy planning docs into the canonical specs taxonomy.

## Key Decisions

- Preserve the legacy spec ID `0010` and slug `usb-pd-sink-toggle` for traceability.
- Keep the original planning scope traceable while assigning long-lived requirements to `SPEC.md` and implementation/history records to companion documents.
- Superseded by `docs/specs/dashboard-extended-voltage-toggle/SPEC.md`, which owns the current Safe5V and extended-voltage control contract.

## Documentation Model

`SPEC.md` is retained as the superseded early 5V/20V implementation contract. Current requirements live in `docs/specs/dashboard-extended-voltage-toggle/SPEC.md`.
