# History

## Origin

- Migrated from `docs/specs/0001-cc-load-switch-toggle/SPEC.md`.
- Legacy source is retained pending delete approval.

## Key Decisions

- Preserve the legacy spec ID `0001` and slug `cc-load-switch-toggle` for traceability.
- Keep the original plan content intact inside `SPEC.md` while adding canonical metadata and companion documents.
